services:
  assets-build:
    image: nitro-converter
    build:
      context: assets
    command: assets-build --build
    env_file:
      - .env
    volumes:
      - ./assets/configuration.json:/app/configuration.json
      - ./assets/assets:/app/assets
    restart: no
    networks: [nitro]


networks:
  nitro:
    external: true